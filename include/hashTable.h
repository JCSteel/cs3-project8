#ifndef HASHTABLE_H
#define HASHTABLE_H

#include <string>

class hashTable
{
    public:
        hashTable();
        virtual ~hashTable();
        int hashNum(string key);

    protected:

    private:
};

#endif // HASHTABLE_H
